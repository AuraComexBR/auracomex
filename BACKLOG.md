# Backlog — Aura Comex

Lista de itens que não são prioridade agora, mas devem ser tratados depois. Toda vez que o Marcos disser "guarda pra depois" / "não é prioridade", o item entra aqui.

- [ ] **Modal de cadastro de cliente sem barra de rolagem**: ajustar o layout do formulário de "Novo Cadastro" (em `/registrations`) pra caber sem precisar de scroll interno, se der pra reorganizar os campos (ex: colunas lado a lado) em vez de depender do `max-h-[90vh] overflow-y-auto` que corrige o overflow mas ainda deixa a rolagem visível em telas menores.
